version: "3"

services:
    app:
        container_name: desafios-front-senai
        build: 
            context: .
            dockerfile: Dockerfile
        ports:
            - "80:3000"
        volumes:
          - .:/app
